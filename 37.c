#include <stdio.h>
#include <math.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    if (a <= b) {
        printf("%d %d", a = 0, b);
    } else {
        printf("%d %d", a, b);
    }
    return 0;
}
