#include <stdio.h>
#include <math.h>

int main() {
    int x;
    double y, z, A, B, C;
    const double e = 2.7182;
    scanf("%d %lf %lf", &x, &y, &z);
    A = pow(2, -x);
    B = sqrt(x + pow(fabs(y) + 2, 1.0/ 4);
    C = pow(pow(e, x - 1) / sin(z + 2) + 2, 1.0/ 3);
    printf("%.2lf", A * B * C);
    return 0;
}
